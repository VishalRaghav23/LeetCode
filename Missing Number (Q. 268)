class Solution {
    public int missingNumber(int[] nums) {
        int numSum =0, arrSum = 0, dis;
        for(int i = 0; i < nums.length; i++){
              numSum += nums[i];
          }
          for(int i = 1; i<=  nums.length; i++){
              arrSum += i;
          }
        dis = arrSum - numSum;
               return dis;
        
    }
}
